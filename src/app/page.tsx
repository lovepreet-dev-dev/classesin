"use client";

import { useDeferredValue, useEffect, useMemo, useState } from "react";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { Activity, ArrowUpRight, Bell, BookOpen, CheckCircle2, ChevronDown, Clock3, FileText, Filter, GraduationCap, LayoutDashboard, Library, Loader2, MoreHorizontal, Plus, Search, Settings, ShieldCheck, Sparkles, Upload, UserPlus, Users, X } from "lucide-react";
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { DEMO_ACTIVITY, DEMO_ALERTS, DEMO_COURSES, DEMO_DASHBOARD, DEMO_ENROLLMENT_EMAILS, DEMO_INSTRUCTORS, DEMO_LEARNERS, type DemoTone } from "@/lib/demo-data";

type Status = "Published" | "Draft" | "Archived";
type Progress = "Not started" | "In progress" | "Completed";
type Course = { id: string; title: string; description: string; category: string; instructor: string; instructorId?: string; status: Status; lessons: number; learners: number; progress?: Progress; updated: string; accent: DemoTone };
type PersonRow = { id: string; name: string; email: string; courses: number; progress: Progress; lastActive: string; tone: DemoTone };
type Alert = { id: string; enrollmentId: string; name: string; initials: string; course: string; days: number; tone: DemoTone };
type BulkResult = { email: string; status: "unknown" | "already_enrolled" | "newly_enrolled" | "error" };
type ActivityItem = { id: string; t: string; d: string; by: string; when: string; event: string };

const hasBackend = Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL && process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY);
const categories = ["All categories", "Compliance", "Leadership", "Skills", "People", "Product", "Operations", "Communication"];

function Badge({ children, tone = "neutral" }: { children: React.ReactNode; tone?: string }) {
  return <span className={`badge badge-${tone.toLowerCase().replaceAll(" ", "-")}`}>{children}</span>;
}

function Avatar({ initials, tone = "lavender" }: { initials: string; tone?: string }) {
  return <span className={`avatar avatar-${tone}`}>{initials}</span>;
}

function ProgressBar({ value, tone = "coral" }: { value: number; tone?: string }) {
  return <div className="progress-track"><div className={`progress-fill fill-${tone}`} style={{ width: `${value}%` }} /></div>;
}

function initialsFor(name: string) {
  return name.split(" ").map((part) => part[0]).join("").slice(0, 2).toUpperCase();
}

function mapCourse(item: { id: string; title: string; description: string; category: string; status: string; updated_at?: string; profiles?: { full_name?: string } | { full_name?: string }[]; enrollments?: { count?: number }[]; lessons?: { count?: number }[] }): Course {
  const profile = Array.isArray(item.profiles) ? item.profiles[0] : item.profiles;
  const learners = Array.isArray(item.enrollments) ? item.enrollments.reduce((total, row) => total + Number(row.count ?? 0), 0) : 0;
  const lessons = Array.isArray(item.lessons) ? item.lessons.reduce((total, row) => total + Number(row.count ?? 0), 0) : 0;
  const fallback = DEMO_COURSES.find((course) => course.id === item.id);
  return { id: item.id, title: item.title, description: item.description, category: item.category, instructor: profile?.full_name ?? fallback?.instructor ?? "Kinship instructor", status: (item.status[0].toUpperCase() + item.status.slice(1)) as Status, lessons, learners, updated: item.updated_at ? new Date(item.updated_at).toLocaleDateString() : "Recently", accent: fallback?.accent ?? ({ Compliance: "coral", Leadership: "lavender", Skills: "mint", People: "blue", Product: "yellow", Operations: "slate", Communication: "coral" } as Record<string, DemoTone>)[item.category] ?? "slate" };
}

export default function Home() {
  const [active, setActive] = useState("Overview");
  const [role, setRole] = useState<"Instructor" | "Learner">("Instructor");
  const [query, setQuery] = useState("");
  const deferredQuery = useDeferredValue(query);
  const [category, setCategory] = useState("All categories");
  const [status, setStatus] = useState("All statuses");
  const [instructorFilter, setInstructorFilter] = useState("All instructors");
  const [sort, setSort] = useState("Newest");
  const [page, setPage] = useState(1);
  const [serverCount, setServerCount] = useState(DEMO_COURSES.length);
  const [showFilters, setShowFilters] = useState(false);
  const [showCreate, setShowCreate] = useState(false);
  const [showBulk, setShowBulk] = useState(false);
  const [alerts, setAlerts] = useState<Alert[]>(DEMO_ALERTS);
  const [courseData, setCourseData] = useState<Course[]>(DEMO_COURSES);
  const [learnerCourseData, setLearnerCourseData] = useState<Course[]>(DEMO_COURSES.filter((course) => course.status === "Published").slice(0, 10));
  const [usingDemoLearnerData, setUsingDemoLearnerData] = useState(true);
  const [people, setPeople] = useState<PersonRow[]>(DEMO_LEARNERS.map((person, index) => ({ id: person.id, name: person.fullName, email: person.email, courses: (index % 5) + 2, progress: index % 4 === 0 ? "Completed" : index % 3 === 0 ? "Not started" : "In progress", lastActive: index % 3 === 0 ? "Not started" : "Aug 18, 2024", tone: person.tone })));
  const [activity, setActivity] = useState<ActivityItem[]>(DEMO_ACTIVITY);
  const [dashboard, setDashboard] = useState(DEMO_DASHBOARD);
  const [demoEnrollmentEmails, setDemoEnrollmentEmails] = useState(DEMO_ENROLLMENT_EMAILS);
  const [usingDemoData, setUsingDemoData] = useState(true);
  const [toast, setToast] = useState("");
  const pageSize = 8;

  const loadingCourses = query !== deferredQuery;

  useEffect(() => {
    if (!hasBackend) return;
    const controller = new AbortController();
    const params = new URLSearchParams({ page: String(page), pageSize: String(pageSize), sort: sort === "Alphabetical" ? "title" : sort === "Learners" ? "enrollment_count" : "created_at" });
    if (deferredQuery) params.set("q", deferredQuery);
    if (category !== "All categories") params.set("category", category);
    if (status !== "All statuses") params.set("status", status.toLowerCase());
    if (instructorFilter !== "All instructors") params.set("instructor", DEMO_INSTRUCTORS.find((person) => person.fullName === instructorFilter)?.id ?? instructorFilter);
    fetch(`/api/courses?${params.toString()}`, { signal: controller.signal })
      .then(async (response) => {
        if (!response.ok) return;
        const payload = await response.json();
        if (!Array.isArray(payload.data)) return;
        setCourseData(payload.data.map(mapCourse)); setServerCount(Number(payload.count ?? payload.data.length)); setUsingDemoData(false);
      })
      .catch((error) => { if (error?.name !== "AbortError") setToast("Showing the demo catalog while the workspace reconnects"); });
    return () => controller.abort();
  }, [deferredQuery, category, status, instructorFilter, sort, page]);

  useEffect(() => {
    if (role !== "Learner" || !hasBackend) return;
    fetch("/api/enrollments").then(async (response) => {
      if (!response.ok) return;
      const payload = await response.json();
      const enrollments = (payload.enrollments ?? []) as { progress: string; courses?: { id: string; title: string; description: string; category: string; status: string; profiles?: { full_name?: string } | { full_name?: string }[]; lessons?: unknown[] } | null }[];
      setLearnerCourseData(enrollments.filter((item) => item.courses).map((item) => { const course = item.courses!; const profile = Array.isArray(course.profiles) ? course.profiles[0] : course.profiles; return { id: course.id, title: course.title, description: course.description, category: course.category, instructor: profile?.full_name ?? "Kinship instructor", status: (course.status[0].toUpperCase() + course.status.slice(1)) as Status, lessons: course.lessons?.length ?? 0, learners: 0, progress: (item.progress[0].toUpperCase() + item.progress.slice(1).replaceAll("_", " ")) as Progress, updated: "Enrolled course", accent: ({ Compliance: "coral", Leadership: "lavender", Skills: "mint", People: "blue", Product: "yellow", Operations: "slate", Communication: "coral" } as Record<string, DemoTone>)[course.category] ?? "slate" }; }));
      setUsingDemoLearnerData(false);
    }).catch(() => undefined);
  }, [role]);

  useEffect(() => {
    if (!hasBackend) return;
    fetch("/api/dashboard").then(async (response) => { if (response.ok) setDashboard(await response.json()); }).catch(() => undefined);
    fetch("/api/alerts").then(async (response) => {
      if (!response.ok) return;
      const payload = await response.json();
      setAlerts((payload.alerts ?? []).map((item: { id: string; last_progress_at?: string; courses?: { title?: string } | { title?: string }[]; profiles?: { full_name?: string } | { full_name?: string }[] }) => {
        const course = Array.isArray(item.courses) ? item.courses[0] : item.courses; const learner = Array.isArray(item.profiles) ? item.profiles[0] : item.profiles; const name = learner?.full_name ?? "Learner";
        return { id: item.id, enrollmentId: item.id, name, initials: initialsFor(name), course: course?.title ?? "Course", days: item.last_progress_at ? Math.max(15, Math.floor((Date.now() - new Date(item.last_progress_at).getTime()) / 86400000)) : 15, tone: (DEMO_LEARNERS.find((person) => person.fullName === name)?.tone ?? "coral") as DemoTone };
      }));
    }).catch(() => undefined);
    fetch("/api/people").then(async (response) => {
      if (!response.ok) return;
      const payload = await response.json();
      setPeople((payload.people ?? []).map((item: { id: string; full_name: string; email: string; courses: number; progress: string; last_active: string | null }, index: number) => ({ id: item.id, name: item.full_name, email: item.email, courses: item.courses, progress: (item.progress[0].toUpperCase() + item.progress.slice(1).replaceAll("_", " ")) as Progress, lastActive: item.last_active ? new Date(item.last_active).toLocaleDateString() : "Not started", tone: DEMO_LEARNERS[index % DEMO_LEARNERS.length]?.tone ?? "lavender" })));
    }).catch(() => undefined);
    fetch("/api/activity").then(async (response) => { if (response.ok) setActivity((await response.json()).activity ?? DEMO_ACTIVITY); }).catch(() => undefined);
  }, []);

  const filtered = useMemo(() => {
    const source = role === "Learner" ? learnerCourseData : courseData;
    if (!usingDemoData && role === "Instructor") return source;
    if (!usingDemoLearnerData && role === "Learner") return source;
    const visible = source.filter((course) => `${course.title} ${course.description}`.toLowerCase().includes(query.toLowerCase()) && (category === "All categories" || course.category === category) && (status === "All statuses" || course.status === status) && (instructorFilter === "All instructors" || course.instructor === instructorFilter) && (role === "Instructor" || course.status === "Published"));
    return [...visible].sort((a, b) => sort === "Alphabetical" ? a.title.localeCompare(b.title) : sort === "Learners" ? b.learners - a.learners : 0);
  }, [category, courseData, instructorFilter, learnerCourseData, query, role, sort, status, usingDemoData, usingDemoLearnerData]);

  function notify(message: string) { setToast(message); window.setTimeout(() => setToast(""), 2800); }
  function clearFilters() { setQuery(""); setCategory("All categories"); setStatus("All statuses"); setInstructorFilter("All instructors"); setSort("Newest"); setPage(1); setShowFilters(false); }

  async function createCourse(input: { title: string; description: string; category: string }) {
    if (!hasBackend) { const item: Course = { id: `demo-${Date.now()}`, ...input, instructor: "Maya Patel", status: "Draft", lessons: 0, learners: 0, updated: "Just now", accent: "coral" }; setCourseData((courses) => [item, ...courses]); setServerCount((count) => count + 1); setShowCreate(false); notify("Draft course created in demo mode"); return; }
    const response = await fetch("/api/courses", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(input) }); const result = await response.json(); if (!response.ok) throw new Error(result.error ?? "Could not create course"); setShowCreate(false); setPage(1); notify("Draft course created");
  }

  async function bulkEnroll(input: { courseId: string; emails: string[] }) {
    if (!hasBackend) {
      const known = new Set(DEMO_LEARNERS.map((person) => person.email));
      const existing = new Set(demoEnrollmentEmails[input.courseId] ?? []);
      const newlyEnrolled = input.emails.filter((email) => known.has(email.toLowerCase()) && !existing.has(email.toLowerCase()));
      setDemoEnrollmentEmails((current) => ({ ...current, [input.courseId]: [...(current[input.courseId] ?? []), ...newlyEnrolled] }));
      setCourseData((courses) => courses.map((course) => course.id === input.courseId ? { ...course, learners: course.learners + newlyEnrolled.length } : course));
      return input.emails.map((email) => ({ email, status: !known.has(email.toLowerCase()) ? "unknown" : existing.has(email.toLowerCase()) ? "already_enrolled" : "newly_enrolled" } as BulkResult));
    }
    const response = await fetch("/api/enrollments/bulk", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(input) }); const result = await response.json(); if (!response.ok) throw new Error(result.error ?? "Could not enroll learners"); setShowBulk(false); notify(`${(result.results ?? []).filter((row: BulkResult) => row.status === "newly_enrolled").length} new learners enrolled`); return result.results ?? [];
  }

  async function dismissAlert(alert: Alert) {
    if (hasBackend && !alert.enrollmentId.startsWith("demo-")) { const response = await fetch("/api/alerts/dismiss", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ enrollmentId: alert.enrollmentId }) }); if (!response.ok) { notify("Could not dismiss that alert"); return; } }
    setAlerts((items) => items.filter((item) => item.id !== alert.id)); notify(`${alert.name}’s alert dismissed`);
  }

  const totalMatches = usingDemoData || role === "Learner" ? filtered.length : serverCount;
  const table = <CourseTable courses={filtered} role={role} loading={loadingCourses} onOpen={(course) => notify(`Open ${course.title} to manage lessons and enrollment`)} />;
  const dashboardBreakdown = <DashboardBreakdown dashboard={dashboard} />;

  return <main className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark"><GraduationCap size={21} /></span><span>kinship<span className="brand-dot">.</span></span></div><button className="workspace-switcher" onClick={() => notify("Workspace switching is available for team accounts.")}><div className="workspace-avatar">N</div><div><strong>Northstar Inc.</strong><small>Learning workspace</small></div><ChevronDown size={15} /></button><p className="eyebrow nav-label">Workspace</p><nav className="primary-nav">{[{ label: "Overview", icon: LayoutDashboard }, { label: "Courses", icon: Library }, { label: "Learners", icon: Users }, { label: "Activity", icon: Activity }].map((item) => { const Icon = item.icon; return <button key={item.label} className={active === item.label ? "nav-item active" : "nav-item"} onClick={() => setActive(item.label)}><Icon size={18} />{item.label}{item.label === "Activity" && alerts.length > 0 && <span className="nav-count">{alerts.length}</span>}</button>; })}</nav><p className="eyebrow nav-label second">Manage</p><nav className="primary-nav"><button className="nav-item" onClick={() => setShowCreate(true)}><Plus size={18} />New course</button><button className="nav-item" onClick={() => notify("Workspace settings are managed by your administrator.")}><Settings size={18} />Settings</button></nav><div className="sidebar-spacer" /><button className="sidebar-help" onClick={() => notify("You’re up to date with the latest Kinship updates.")}><Sparkles size={17} /><div><strong>Make learning stick</strong><span>See what’s new in Kinship</span></div><ArrowUpRight size={15} /></button><div className="profile-row"><Avatar initials="MP" tone="coral" /><div><strong>Maya Patel</strong><small>Instructor</small></div><button className="icon-button subtle" aria-label="Profile menu" onClick={() => notify("Profile menu")}><MoreHorizontal size={17} /></button></div></aside><section className="main-content"><header className="topbar"><div className="breadcrumbs"><span>Workspace</span><span>/</span><strong>{active}</strong></div><div className="topbar-actions"><div className="role-toggle" aria-label="Preview role"><button className={role === "Instructor" ? "selected" : ""} onClick={() => setRole("Instructor")}>Instructor</button><button className={role === "Learner" ? "selected" : ""} onClick={() => setRole("Learner")}>Learner view</button></div><button className="icon-button" aria-label="Notifications" onClick={() => setActive("Activity")}><Bell size={19} />{alerts.length > 0 && <span className="notification-dot" />}</button><button className="icon-button" aria-label="Open profile" onClick={() => notify("Profile menu")}><Avatar initials="MP" tone="coral" /></button></div></header><div className="content-wrap">{active === "Overview" ? <><div className="page-heading"><div><p className="eyebrow">Tuesday, September 3, 2024</p><h1>Good morning, Maya <span className="wave">✦</span></h1><p className="subhead">Here’s what’s happening across your learning workspace.</p></div><button className="button button-primary" onClick={() => setShowCreate(true)}><Plus size={17} /> Create course</button></div><div className="metric-grid"><MetricCard label="Total learners" value={String(dashboard.totalLearners)} delta="+12.5%" detail="vs. last month" icon={<Users size={18} />} tone="lavender" /><MetricCard label="Published courses" value={String(dashboard.publishedCourses)} delta="+3" detail="this quarter" icon={<BookOpen size={18} />} tone="mint" /><MetricCard label="Completions this month" value={String(dashboard.completionsThisMonth)} delta="+18.2%" detail="vs. last month" icon={<CheckCircle2 size={18} />} tone="yellow" /><MetricCard label="In progress" value={String(dashboard.inProgress)} delta="-8.4%" detail="vs. last month" icon={<Clock3 size={18} />} tone="coral" /></div><div className="dashboard-grid"><section className="panel chart-panel"><div className="panel-header"><div><p className="eyebrow">Momentum</p><h2>Completions over time</h2></div><button className="select-button" onClick={() => notify("Showing the last 8 weeks")}>Last 8 weeks <ChevronDown size={15} /></button></div><div className="chart-legend"><span><i className="legend-dot coral-dot" />Completions</span><span className="chart-total">{dashboard.completionTotal} <small>total</small></span></div><div className="chart-wrap"><ResponsiveContainer width="100%" height="100%"><AreaChart data={dashboard.weekly} margin={{ top: 10, right: 8, left: -28, bottom: 0 }}><defs><linearGradient id="coralGradient" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#f47f6b" stopOpacity={0.32} /><stop offset="100%" stopColor="#f47f6b" stopOpacity={0.02} /></linearGradient></defs><CartesianGrid vertical={false} stroke="#eeeae4" /><XAxis dataKey="week" tickLine={false} axisLine={false} tick={{ fill: "#918b83", fontSize: 11 }} /><YAxis tickLine={false} axisLine={false} tick={{ fill: "#918b83", fontSize: 11 }} /><Tooltip contentStyle={{ borderRadius: 12, border: "1px solid #ebe6df", boxShadow: "0 8px 24px rgba(47,43,38,.1)" }} /><Area type="monotone" dataKey="completions" stroke="#e96e5a" strokeWidth={2.5} fill="url(#coralGradient)" /></AreaChart></ResponsiveContainer></div></section><section className="panel alerts-panel"><div className="panel-header"><div><p className="eyebrow">Needs a nudge</p><h2>Inactivity alerts <span className="count-pill">{alerts.length}</span></h2></div><button className="text-button" onClick={() => setActive("Activity")}>View all <ArrowUpRight size={14} /></button></div><p className="panel-caption">Learners who haven’t made progress in 14+ days.</p><div className="alert-list">{alerts.map((alert) => <div className="alert-row" key={alert.id}><Avatar initials={alert.initials} tone={alert.tone} /><div className="alert-copy"><strong>{alert.name}</strong><span>{alert.course}</span></div><span className="alert-days">{alert.days}d quiet</span><button className="dismiss-button" aria-label={`Dismiss ${alert.name} alert`} onClick={() => dismissAlert(alert)}><X size={14} /></button></div>)}</div>{alerts.length === 0 && <div className="empty-alerts"><ShieldCheck size={24} /><span>All caught up for now.</span></div>}</section></div>{dashboardBreakdown}<section className="panel course-panel"><div className="panel-header course-panel-header"><div><p className="eyebrow">Your library</p><h2>Courses <span className="count-pill soft">{totalMatches}</span></h2></div><button className="text-button" onClick={() => setActive("Courses")}>Manage courses <ArrowUpRight size={14} /></button></div><CourseToolbar query={query} setQuery={setQuery} category={category} setCategory={setCategory} status={status} setStatus={setStatus} instructorFilter={instructorFilter} setInstructorFilter={setInstructorFilter} sort={sort} setSort={setSort} showFilters={showFilters} setShowFilters={setShowFilters} clearFilters={clearFilters} />{table}</section></> : <SecondaryView active={active} filtered={filtered} totalMatches={totalMatches} page={page} pageSize={pageSize} setPage={setPage} role={role} query={query} setQuery={setQuery} category={category} setCategory={setCategory} status={status} setStatus={setStatus} instructorFilter={instructorFilter} setInstructorFilter={setInstructorFilter} sort={sort} setSort={setSort} showFilters={showFilters} setShowFilters={setShowFilters} clearFilters={clearFilters} people={people} activity={activity} alerts={alerts} onCreate={() => setShowCreate(true)} onBulk={() => setShowBulk(true)} onOpen={notify} serverPaginated={!usingDemoData && role === "Instructor"} />}</div></section>{showCreate && <CreateModal onClose={() => setShowCreate(false)} onSave={createCourse} />}{showBulk && <BulkModal courses={courseData.filter((course) => course.status === "Published")} onClose={() => setShowBulk(false)} onSave={bulkEnroll} />}{toast && <div className="toast" role="status"><CheckCircle2 size={17} />{toast}</div>}</main>;
}

function MetricCard({ label, value, delta, detail, icon, tone }: { label: string; value: string; delta: string; detail: string; icon: React.ReactNode; tone: string }) { return <div className="metric-card"><div className={`metric-icon icon-${tone}`}>{icon}</div><p>{label}</p><div className="metric-value">{value}</div><div className="metric-change"><span>{delta}</span> {detail}</div></div>; }

function CourseToolbar({ query, setQuery, category, setCategory, status, setStatus, instructorFilter, setInstructorFilter, sort, setSort, showFilters, setShowFilters, clearFilters }: { query: string; setQuery: (value: string) => void; category: string; setCategory: (value: string) => void; status: string; setStatus: (value: string) => void; instructorFilter: string; setInstructorFilter: (value: string) => void; sort: string; setSort: (value: string) => void; showFilters: boolean; setShowFilters: (value: boolean) => void; clearFilters: () => void }) { return <div className="toolbar filter-toolbar"><div className="search-box"><Search size={17} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search courses" /></div><button className={`filter-button ${showFilters ? "active" : ""}`} onClick={() => setShowFilters(!showFilters)}><Filter size={15} /> Filters <ChevronDown size={13} /></button><select aria-label="Course category" className="select-button native-select" value={category} onChange={(event) => setCategory(event.target.value)}>{categories.map((item) => <option key={item}>{item}</option>)}</select><select aria-label="Course sort" className="select-button native-select" value={sort} onChange={(event) => setSort(event.target.value)}><option>Newest</option><option>Alphabetical</option><option>Learners</option></select>{showFilters && <div className="filter-popover"><label>Status<select value={status} onChange={(event) => setStatus(event.target.value)}><option>All statuses</option><option>Published</option><option>Draft</option><option>Archived</option></select></label><label>Instructor<select value={instructorFilter} onChange={(event) => setInstructorFilter(event.target.value)}><option>All instructors</option>{DEMO_INSTRUCTORS.map((person) => <option key={person.id}>{person.fullName}</option>)}</select></label><button className="text-button" onClick={clearFilters}>Clear filters</button></div>}</div>; }

function CourseTable({ courses, role, loading, onOpen }: { courses: Course[]; role: "Instructor" | "Learner"; loading: boolean; onOpen: (course: Course) => void }) { return <div className="course-table"><div className="table-head"><span>Course</span><span>Status</span><span>Learners</span><span>Progress</span><span>Last updated</span><span /></div>{loading ? <CourseTableSkeleton /> : courses.length ? courses.map((course) => <CourseRow key={course.id} course={course} role={role} onOpen={() => onOpen(course)} />) : <EmptyCourseState />}</div>; }

function CourseRow({ course, role, onOpen }: { course: Course; role: "Instructor" | "Learner"; onOpen: () => void }) { const router = useRouter(); const values: Record<string, number> = { "Not started": 0, "In progress": 58, Completed: 100 }; const open = () => router.push(`/courses/${course.id}`); return <div className="course-row" role="link" tabIndex={0} onClick={open} onKeyDown={(event) => { if (event.key === "Enter" || event.key === " ") { event.preventDefault(); open(); } }}><div className="course-title-cell"><div className={`course-thumb thumb-${course.accent}`}><FileText size={19} /></div><div><Link href={`/courses/${course.id}`} className="course-link" onClick={(event) => event.stopPropagation()}>{course.title}</Link><span>{course.category} <i /> {course.lessons} lessons</span></div></div><div><Badge tone={course.status}>{course.status}</Badge></div><div className="learner-count"><Users size={14} /> {course.learners}</div><div className="progress-cell"><ProgressBar value={role === "Learner" ? values[course.progress ?? "Not started"] : course.status === "Published" ? 72 : 0} tone={course.progress === "Completed" ? "mint" : "coral"} /><small>{role === "Learner" ? course.progress : course.status === "Published" ? "72% avg." : "—"}</small></div><div className="updated-cell">{course.updated}</div><button className="row-menu" aria-label={`More actions for ${course.title}`} onClick={(event) => { event.stopPropagation(); onOpen(); }}><MoreHorizontal size={17} /></button></div>; }

function CourseTableSkeleton() { return <>{[1, 2, 3, 4].map((item) => <div className="course-row skeleton-row" key={item}><span /><span /><span /><span /><span /><span /></div>)}</>; }
function EmptyCourseState() { return <div className="empty-course"><Search size={22} /><strong>No courses match those filters</strong><span>Try a different search or reset the filters.</span></div>; }

function SecondaryView({ active, filtered, totalMatches, page, pageSize, setPage, role, query, setQuery, category, setCategory, status, setStatus, instructorFilter, setInstructorFilter, sort, setSort, showFilters, setShowFilters, clearFilters, people, activity, alerts, onCreate, onBulk, onOpen, serverPaginated }: { active: string; filtered: Course[]; totalMatches: number; page: number; pageSize: number; setPage: (value: number) => void; role: "Instructor" | "Learner"; query: string; setQuery: (value: string) => void; category: string; setCategory: (value: string) => void; status: string; setStatus: (value: string) => void; instructorFilter: string; setInstructorFilter: (value: string) => void; sort: string; setSort: (value: string) => void; showFilters: boolean; setShowFilters: (value: boolean) => void; clearFilters: () => void; people: PersonRow[]; activity: ActivityItem[]; alerts: Alert[]; onCreate: () => void; onBulk: () => void; onOpen: (message: string) => void; serverPaginated: boolean }) { const totalPages = Math.max(1, Math.ceil(totalMatches / pageSize)); const visibleCourses = serverPaginated ? filtered : filtered.slice((page - 1) * pageSize, page * pageSize); return <div className="secondary-view"><div className="page-heading"><div><p className="eyebrow">Workspace</p><h1>{active}</h1><p className="subhead">{active === "Courses" ? "Build, publish, and improve learning that moves your team forward." : active === "Learners" ? "See who is learning, progressing, and ready for recognition." : "A permanent record of course changes and learner activity."}</p></div>{active !== "Activity" && <button className="button button-primary" onClick={active === "Courses" ? onCreate : onBulk}>{active === "Courses" ? <><Plus size={17} /> Create course</> : <><Upload size={17} /> Bulk enroll</>}</button>}</div>{active === "Courses" ? <section className="panel course-panel"><CourseToolbar query={query} setQuery={setQuery} category={category} setCategory={setCategory} status={status} setStatus={setStatus} instructorFilter={instructorFilter} setInstructorFilter={setInstructorFilter} sort={sort} setSort={setSort} showFilters={showFilters} setShowFilters={setShowFilters} clearFilters={clearFilters} /><CourseTable courses={visibleCourses} role={role} loading={false} onOpen={(course) => onOpen(`Open ${course.title} to manage lessons and enrollment`)} /><div className="pagination"><span>Showing {totalMatches ? ((page - 1) * pageSize) + 1 : 0}–{Math.min(page * pageSize, totalMatches)} of {totalMatches} courses</span><div><button disabled={page <= 1} onClick={() => setPage(Math.max(1, page - 1))}>‹</button><button className="current">{page}</button><button disabled={page >= totalPages} onClick={() => setPage(Math.min(totalPages, page + 1))}>›</button></div></div></section> : active === "Learners" ? role === "Instructor" ? <LearnersView people={people} onBulk={onBulk} /> : <AccessNotice message="Learner profiles are private to instructors." /> : <ActivityView activity={activity} alerts={alerts} onFilter={() => onOpen("Activity filters are ready for the next review.")} />}</div>; }

function AccessNotice({ message }: { message: string }) { return <section className="panel access-notice"><ShieldCheck size={26} /><h2>Private workspace area</h2><p>{message}</p></section>; }

function LearnersView({ people, onBulk }: { people: PersonRow[]; onBulk: () => void }) { const [search, setSearch] = useState(""); const visible = people.filter((person) => `${person.name} ${person.email}`.toLowerCase().includes(search.toLowerCase())); return <section className="panel course-panel"><div className="panel-header"><div><p className="eyebrow">{people.length} total learners</p><h2>All learners</h2></div><button className="button button-secondary" onClick={onBulk}><Upload size={16} /> Bulk enroll</button></div><div className="toolbar"><div className="search-box"><Search size={17} /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search learners" /></div></div><div className="learner-table"><div className="table-head"><span>Learner</span><span>Courses</span><span>Latest status</span><span>Last active</span><span /></div>{visible.map((person) => <div className="learner-row" key={person.email}><div className="course-title-cell"><Avatar initials={initialsFor(person.name)} tone={person.tone} /><div><strong>{person.name}</strong><span>{person.email}</span></div></div><span>{person.courses} courses</span><Badge tone={person.progress}>{person.progress}</Badge><span className="updated-cell">{person.lastActive}</span><button className="icon-button subtle" aria-label={`More actions for ${person.name}`}><MoreHorizontal size={17} /></button></div>)}</div></section>; }

function ActivityView({ activity, alerts, onFilter }: { activity: ActivityItem[]; alerts: Alert[]; onFilter: () => void }) { return <section className="panel course-panel"><div className="panel-header"><div><p className="eyebrow">Immutable history</p><h2>Activity log</h2></div><button className="filter-button" onClick={onFilter}><Filter size={15} /> Filter activity</button></div><p className="panel-caption">Every publish, edit, enrollment, completion, comment, and alert dismissal is recorded.</p>{alerts.length > 0 && <div className="activity-alert-summary"><Bell size={15} /> {alerts.length} active inactivity {alerts.length === 1 ? "alert" : "alerts"} <span>· dismiss from Overview</span></div>}<div className="activity-list">{activity.map((item) => <div className="activity-row" key={item.id}><div className="activity-icon">{item.event === "lesson_completed" ? <CheckCircle2 size={16} /> : item.event === "enrolled" ? <UserPlus size={16} /> : item.event === "archived" ? <Activity size={16} /> : <BookOpen size={16} />}</div><div><strong>{item.t}</strong><p>{item.d}</p><span>By {item.by}</span></div><time>{item.when}</time></div>)}</div></section>; }

function CreateModal({ onClose, onSave }: { onClose: () => void; onSave: (input: { title: string; description: string; category: string }) => Promise<void> }) { const [title, setTitle] = useState(""); const [description, setDescription] = useState(""); const [category, setCategory] = useState("Compliance"); const [saving, setSaving] = useState(false); const [error, setError] = useState(""); async function save() { setSaving(true); setError(""); try { await onSave({ title: title.trim(), description: description.trim(), category }); } catch (e) { setError(e instanceof Error ? e.message : "Could not create course"); } finally { setSaving(false); } } return <div className="modal-backdrop"><div className="modal" role="dialog" aria-modal="true" aria-labelledby="create-course-title"><div className="modal-header"><div><p className="eyebrow">Course builder</p><h2 id="create-course-title">Create a course</h2></div><button className="icon-button subtle" aria-label="Close dialog" onClick={onClose}><X size={18} /></button></div><p className="modal-copy">Start with the essentials. Add lessons in the course builder before publishing.</p><label>Course title<input required value={title} onChange={(event) => setTitle(event.target.value)} placeholder="e.g. Security foundations" /></label><label>Description<textarea value={description} onChange={(event) => setDescription(event.target.value)} placeholder="What will learners be able to do after this course?" rows={3} /></label><div className="two-fields"><label>Category<select value={category} onChange={(event) => setCategory(event.target.value)}>{categories.slice(1).map((item) => <option key={item}>{item}</option>)}</select></label><label>Owner<select disabled><option>Maya Patel</option></select></label></div>{error && <p className="modal-error">{error}</p>}<div className="modal-actions"><button className="button button-secondary" onClick={onClose}>Cancel</button><button className="button button-primary" disabled={!title.trim() || saving} onClick={save}>{saving ? <><Loader2 size={16} className="spin" /> Creating…</> : <><Plus size={16} /> Create draft</>}</button></div></div></div>; }

function BulkModal({ courses, onClose, onSave }: { courses: Course[]; onClose: () => void; onSave: (input: { courseId: string; emails: string[] }) => Promise<BulkResult[]> }) { const [courseId, setCourseId] = useState(courses[0]?.id ?? ""); const [emails, setEmails] = useState(""); const [saving, setSaving] = useState(false); const [results, setResults] = useState<BulkResult[] | null>(null); const [error, setError] = useState(""); function importCsv(event: React.ChangeEvent<HTMLInputElement>) { const file = event.target.files?.[0]; if (!file) return; const reader = new FileReader(); reader.onload = () => setEmails(String(reader.result ?? "").split(/\r?\n/).slice(1).map((line) => line.split(",")[0]?.trim()).filter(Boolean).join("\n")); reader.readAsText(file); } async function save() { setSaving(true); setError(""); try { const result = await onSave({ courseId, emails: [...new Set(emails.split(/[\n,;]+/).map((email) => email.trim().toLowerCase()).filter(Boolean))] }); setResults(result); } catch (e) { setError(e instanceof Error ? e.message : "Could not enroll learners"); } finally { setSaving(false); } } const statusLabel: Record<BulkResult["status"], string> = { newly_enrolled: "Newly enrolled", already_enrolled: "Already enrolled", unknown: "Unknown address", error: "Could not process" }; return <div className="modal-backdrop"><div className="modal bulk-modal" role="dialog" aria-modal="true" aria-labelledby="bulk-title"><div className="modal-header"><div><p className="eyebrow">Enrollment tools</p><h2 id="bulk-title">Bulk enroll learners</h2></div><button className="icon-button subtle" aria-label="Close dialog" onClick={onClose}><X size={18} /></button></div>{results ? <><p className="modal-copy">Each address has been checked. Nothing is hidden when an address is unknown or already enrolled.</p><div className="bulk-summary"><strong>{results.filter((row) => row.status === "newly_enrolled").length} newly enrolled</strong><span>{results.filter((row) => row.status === "already_enrolled").length} already enrolled</span><span>{results.filter((row) => row.status === "unknown").length} unknown</span></div><div className="bulk-results">{results.map((row) => <div key={row.email}><span>{row.email}</span><Badge tone={row.status === "newly_enrolled" ? "Completed" : row.status === "unknown" ? "Archived" : "Draft"}>{statusLabel[row.status]}</Badge></div>)}</div><div className="modal-actions"><button className="button button-primary" onClick={onClose}>Done</button></div></> : <><p className="modal-copy">Paste one email per line or upload a CSV. We’ll check each address and report the result.</p><label>Course<select value={courseId} onChange={(event) => setCourseId(event.target.value)}>{courses.map((course) => <option key={course.id} value={course.id}>{course.title}</option>)}</select></label><label>Email addresses<textarea value={emails} onChange={(event) => setEmails(event.target.value)} placeholder={'learner@northstar.co\nnew.joiner@northstar.co'} rows={5} /></label><label className="upload-drop"><Upload size={18} /><span><strong>Upload CSV</strong><small>or choose a file from your computer</small></span><input type="file" accept=".csv,text/csv" onChange={importCsv} /></label>{error && <p className="modal-error">{error}</p>}<div className="modal-actions"><button className="button button-secondary" onClick={onClose}>Cancel</button><button className="button button-primary" disabled={!courseId || !emails.trim() || saving} onClick={save}>{saving ? <><Loader2 size={16} className="spin" /> Reviewing…</> : <><Users size={16} /> Review enrollments</>}</button></div></>}</div></div>; }

function DashboardBreakdown({ dashboard }: { dashboard: typeof DEMO_DASHBOARD }) {
  const labels: Record<string, string> = { completed: "Completed", in_progress: "In progress", not_started: "Not started" };
  const totalProgress = dashboard.progressBreakdown.reduce((sum, item) => sum + item.count, 0);
  return <div className="dashboard-breakdown-grid"><section className="panel breakdown-panel"><div className="panel-header"><div><p className="eyebrow">Enrollment mix</p><h2>By course</h2></div><span className="breakdown-caption">{dashboard.courseBreakdown.length} courses</span></div><div className="breakdown-course-list">{dashboard.courseBreakdown.slice(0, 6).map((item) => <div className="breakdown-course" key={item.course}><div className="breakdown-course-heading"><strong>{item.course}</strong><span>{item.enrolled} enrolled</span></div><div className="breakdown-bar"><i style={{ width: String(item.enrolled ? (item.completed / item.enrolled) * 100 : 0) + "%" }} /><i style={{ width: String(item.enrolled ? (item.inProgress / item.enrolled) * 100 : 0) + "%" }} /><i style={{ width: String(item.enrolled ? (item.notStarted / item.enrolled) * 100 : 0) + "%" }} /></div><small>{item.completed} completed · {item.inProgress} in progress · {item.notStarted} not started</small></div>)}</div><p className="breakdown-footnote">Showing the six courses with the most enrollments.</p></section><section className="panel breakdown-panel"><div className="panel-header"><div><p className="eyebrow">Progress state</p><h2>Across all enrollments</h2></div><span className="breakdown-caption">{totalProgress} total</span></div><div className="progress-breakdown-list">{dashboard.progressBreakdown.map((item) => <div className="progress-breakdown-row" key={item.progress}><div><span className={"progress-dot progress-" + item.progress} /><strong>{labels[item.progress] ?? item.progress}</strong></div><strong>{item.count}</strong></div>)}</div><div className="progress-total-bar">{dashboard.progressBreakdown.map((item) => <i key={item.progress} className={"progress-" + item.progress} style={{ width: String(totalProgress ? (item.count / totalProgress) * 100 : 0) + "%" }} />)}</div><p className="breakdown-footnote">A learner’s state is tracked separately for every enrolled course.</p></section></div>;
}
